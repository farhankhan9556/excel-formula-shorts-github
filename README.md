# Learn Verse Excel Shorts

Windows self-hosted runner + Microsoft Excel Desktop + pyautogui + FFmpeg + Edge TTS.

Run first test with count=1. The Windows desktop must be logged in and unlocked because the workflow controls the real Excel GUI.
